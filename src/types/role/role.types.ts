export enum RoleStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  SUSPENDED = "SUSPENDED",
}

export enum SystemRole {
  SUPER_ADMIN = "super-admin",
  ORG_ADMIN = "org-admin",
  MANAGER = "manager",
  EMPLOYEE = "employee",
  USER = "user",
}
